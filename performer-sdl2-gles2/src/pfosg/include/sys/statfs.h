/* sys/statfs.h - pfosg stub (macOS uses sys/mount.h; the samples only use
 * statfs in IRIX-specific free-memory queries that degrade gracefully). */
#ifndef PFOSG_STATFS_H
#define PFOSG_STATFS_H
#include <sys/param.h>
#include <sys/mount.h>
#endif
